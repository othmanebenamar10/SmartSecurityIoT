PySide6==6.7.2
opencv-contrib-python==4.10.0.84
numpy==2.0.2
python-dotenv==1.0.1
bcrypt>=4.0.0,<6.0.0
requests==2.32.3

# Optional (enabled automatically if installed):
# face_recognition (requires dlib build wheels; may be heavy)
# deepface (heavy ML deps)
# pymodbus (for Siemens LOGO! via Modbus TCP)
# plyer (for Windows native notifications)
